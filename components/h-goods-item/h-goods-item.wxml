<!--components/h-goods-item/h-goods-item.wxml-->
<view class="goods-item" bind:tap="itemClick" data-iid="{{item.iid}}">
  <image src="{{item.show.img || item.image || item.img}}" class="image" mode="widthFix" lazy-load/>
  <view class="desc-info">
    <view class="title">{{item.title}}</view>
    <view class="info">
      <text class="price">￥{{item.price}}</text>
      <image class="icon" src="/assets/images/common/favor.png" />
      <text class="cfav">{{item.cfav}}</text>
    </view>
  </view>
</view>
